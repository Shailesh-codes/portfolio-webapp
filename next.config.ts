import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["10.165.205.236", "localhost:3001", "localhost:3000", "localhost:3002"],
};

export default nextConfig;
